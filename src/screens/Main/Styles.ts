import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    marginTop: 20,
  },

  img: {
    flex: 1,
    resizeMode: 'cover',
    justifyContent: 'center',
  },

  btnWrapper: {
    marginTop: 50,
    gap: 20,
  },
});
